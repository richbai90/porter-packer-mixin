config:
  clientVersion: v0.0.0
install:
  - packer:
      description: "Summon Minion"
      arguments:
      - "man-e-faces"
      flags:
        species: "human"
      outputs:
        - name: "PASS"
          jsonPath: "$Id"
